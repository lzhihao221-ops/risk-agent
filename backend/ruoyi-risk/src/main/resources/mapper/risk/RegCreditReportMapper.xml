<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.risk.mapper.RegCreditReportMapper">
    <resultMap type="com.ruoyi.risk.domain.RegCreditReport" id="RegCreditReportResult">
        <result property="id" column="id"/>
        <result property="reportPeriod" column="report_period"/>
        <result property="reportDate" column="report_date"/>
        <result property="reportType" column="report_type"/>
        <result property="dataType" column="data_type"/>
        <result property="recordCount" column="record_count"/>
        <result property="successCount" column="success_count"/>
        <result property="failCount" column="fail_count"/>
        <result property="fileName" column="file_name"/>
        <result property="filePath" column="file_path"/>
        <result property="status" column="status"/>
        <result property="errorDetail" column="error_detail"/>
        <result property="submitUser" column="submit_user"/>
        <result property="submitTime" column="submit_time"/>
        <result property="batchNo" column="batch_no"/>
        <result property="remark" column="remark"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectById" parameterType="Long" resultMap="RegCreditReportResult">
        select * from reg_credit_report where id = #{id}
    </select>

    <select id="selectList" parameterType="com.ruoyi.risk.domain.RegCreditReport" resultMap="RegCreditReportResult">
        select * from reg_credit_report
        <where>
            <if test="reportPeriod != null and reportPeriod != ''">and report_period = #{reportPeriod}</if>
            <if test="reportType != null and reportType != ''">and report_type = #{reportType}</if>
            <if test="dataType != null and dataType != ''">and data_type = #{dataType}</if>
            <if test="status != null">and status = #{status}</if>
        </where>
        order by report_period desc, data_type
    </select>

    <insert id="insert" parameterType="com.ruoyi.risk.domain.RegCreditReport" useGeneratedKeys="true" keyProperty="id">
        insert into reg_credit_report (report_period, report_date, report_type, data_type, record_count, success_count, fail_count, file_name, file_path, status, error_detail, submit_user, submit_time, batch_no, remark, create_by)
        values (#{reportPeriod}, #{reportDate}, #{reportType}, #{dataType}, #{recordCount}, #{successCount}, #{failCount}, #{fileName}, #{filePath}, #{status}, #{errorDetail}, #{submitUser}, #{submitTime}, #{batchNo}, #{remark}, #{createBy})
    </insert>

    <update id="update" parameterType="com.ruoyi.risk.domain.RegCreditReport">
        update reg_credit_report
        <set>
            <if test="recordCount != null">record_count = #{recordCount},</if>
            <if test="successCount != null">success_count = #{successCount},</if>
            <if test="failCount != null">fail_count = #{failCount},</if>
            <if test="status != null">status = #{status},</if>
            <if test="errorDetail != null">error_detail = #{errorDetail},</if>
            <if test="submitUser != null">submit_user = #{submitUser},</if>
            <if test="submitTime != null">submit_time = #{submitTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteByIds" parameterType="String">
        delete from reg_credit_report where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
