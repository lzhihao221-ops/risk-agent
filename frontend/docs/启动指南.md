# 银行贷后风险监测工作台 - 启动指南

## 一、环境准备

### 1.1 基础环境

| 工具 | 版本要求 | 说明 |
|------|----------|------|
| JDK | 17+ | Spring Boot 3.x 要求 |
| Maven | 3.8+ | 项目构建 |
| MySQL | 8.0+ | 数据库 |
| Redis | 6.0+ | 缓存 |
| Node.js | 18+ | 前端构建 |
| Git | 2.30+ | 版本控制 |

### 1.2 环境检查

```bash
# 检查 JDK
java -version

# 检查 Maven
mvn -version

# 检查 MySQL
mysql --version

# 检查 Redis
redis-server --version

# 检查 Node.js
node -v
npm -v
```

## 二、获取源码

```bash
# 克隆项目仓库
git clone <your-repo-url> risk-monitor

# 进入项目目录
cd risk-monitor

# 查看项目结构
ls -la
```

## 三、初始化数据库

```bash
# 登录 MySQL
mysql -u root -p

# 执行基础表（项目自带的 SQL 文件）
source sql/ry_20250522.sql

# 执行风控业务表（我们新建的）
source docs/数据库初始化.sql

# 验证表是否创建成功
USE risk_monitor;
SHOW TABLES;
```

## 四、后端配置与启动

### 4.1 修改数据库配置

编辑 `risk-monitor-backend/risk-admin/src/main/resources/application-dev.yml`：

```yaml
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    druid:
      url: jdbc:mysql://localhost:3306/risk_monitor?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
      username: root
      password: your_password
  redis:
    host: localhost
    port: 6379
    password: 
```

### 4.2 启动后端

```bash
# 方式一：IDEA 启动
# 打开项目 → 找到 RiskAgentApplication.java → 右键 Run

# 方式二：命令行启动
mvn clean package -DskipTests
java -jar risk-monitor-backend/risk-admin/target/risk-admin.jar

# 访问后端 API
# http://localhost:8080
# 默认账号：admin / admin123
```

## 五、前端配置与启动

```bash
# 进入前端目录
cd risk-monitor

# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 访问前端
# http://localhost:80
# 默认账号：admin / admin123
```

## 六、创建风控业务模块

### 6.1 后端模块结构

```
risk-agent-risk/
├── src/main/java/com/riskagent/risk/
│   ├── controller/           # 接口层
│   │   ├── RiskCompanyController.java
│   │   ├── RiskEventController.java
│   │   ├── RiskAlertController.java
│   │   └── RiskTaskController.java
│   ├── service/              # 业务层
│   │   ├── IRiskCompanyService.java
│   │   ├── IRiskEventService.java
│   │   ├── IRiskAlertService.java
│   │   └── IRiskTaskService.java
│   ├── mapper/               # 数据访问层
│   │   ├── RiskCompanyMapper.java
│   │   ├── RiskEventMapper.java
│   │   ├── RiskAlertMapper.java
│   │   └── RiskTaskMapper.java
│   ├── domain/               # 实体类
│   │   ├── RiskCompany.java
│   │   ├── RiskEvent.java
│   │   ├── RiskAlertRule.java
│   │   └── RiskCheckTask.java
│   └── provider/             # 数据源适配器
│       ├── DataProviderAdapter.java
│       ├── TianyanchaProvider.java
│       └── JudicialProvider.java
└── src/main/resources/
    └── mapper/risk/          # MyBatis XML
        ├── RiskCompanyMapper.xml
        ├── RiskEventMapper.xml
        └── RiskAlertMapper.xml
```

### 6.2 前端页面结构

```
risk-monitor/src/views/risk/
├── dashboard/                # 风控看板
│   └── index.vue
├── company/                  # 企业管理
│   ├── index.vue             # 企业列表
│   ├── detail.vue            # 企业画像
│   └── relation.vue          # 关联图谱
├── alert/                    # 预警中心
│   ├── index.vue             # 预警列表
│   └── rule.vue              # 规则配置
├── task/                     # 任务管理
│   └── index.vue
└── scan/                     # 批量扫描
    └── index.vue
```

## 七、开发路线

### Week 1：基础框架 + 企业 CRUD
- [ ] 初始化项目结构
- [ ] 创建风控模块
- [ ] 实现企业信息 CRUD
- [ ] 企业列表页面

### Week 2：风险评分 + 事件管理
- [ ] 风险评分算法
- [ ] 风险事件管理
- [ ] 企业画像页面
- [ ] 预警规则配置

### Week 3：预警系统 + 任务管理
- [ ] 预警触发逻辑
- [ ] 预警列表与处理
- [ ] 贷后检查任务
- [ ] 任务到期提醒

### Week 4：数据源对接 + 关联图谱
- [ ] 天眼查 API 对接
- [ ] 司法数据对接
- [ ] 关联图谱可视化
- [ ] 风控看板

## 八、常用命令

```bash
# 后端打包
mvn clean package -DskipTests

# 后端热部署（IDEA 安装 JRebel 插件）

# 前端构建生产版本
cd risk-monitor
npm run build:prod

# Docker 部署
docker-compose up -d
```

## 九、注意事项

1. **数据安全**：银行数据不能出域，所有数据存储在本地数据库
2. **API 费用**：天眼查/企查查 API 按次收费，开发阶段用 Mock 数据
3. **权限控制**：内置 RBAC 权限体系，增加风控相关角色
4. **日志审计**：所有操作必须记录日志，满足银行审计要求
